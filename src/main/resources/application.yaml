spring:
  application:
    name: withiy-server
  profiles:
    group:
      deploy:
        - deploy
        - jwt
      test:
        - test
        - jwt
    active: deploy
springdoc:
  swagger-ui:
    path: /swagger
  api-docs:
    path: /api-docs
  paths-to-match: /**