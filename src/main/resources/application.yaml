spring:
  application:
    name: withiy-server
  profiles:
    group:
      deploy:
        - deploy
        - jwt
      test:
        - test
        - jwt
    active: deploy
springdoc:
  swagger-ui:
    path: /swagger
  api-docs:
    path: /api-docs
hmac:
  secret-key: ${HMAC_SECRET_KEY}

